# 可复用代码与废弃代码标记

## 一、通用操作逻辑（可复用代码）

### 1. GUI操作模块

| 文件路径 | 函数/模块 | 标记 | 复用说明 |
|---------|---------|---------|---------|
| `utils/GUIUtils.py` | `clickByImg()` | ✅ 可复用 | 通用图片定位点击功能，适用于任何Windows桌面应用 |
| `utils/GUIUtils.py` | `DoubleclickByImg()` | ✅ 可复用 | 通用图片定位双击功能 |
| `utils/GUIUtils.py` | `rightClickByImg()` | ✅ 可复用 | 通用图片定位右键点击功能 |
| `utils/GUIUtils.py` | `waitUntilShow()` | ✅ 可复用 | 通用等待图片出现功能 |
| `utils/GUIUtils.py` | `wait_for_image_to_disappear()` | ✅ 可复用 | 通用等待图片消失功能 |
| `utils/GUIUtils.py` | `clickUntilShow()` | ✅ 可复用 | 通用点击直到图片出现功能 |
| `utils/GUIUtils.py` | `enterUntilShow()` | ✅ 可复用 | 通用回车直到图片出现功能 |
| `utils/GUIUtils.py` | `maxWindowUntilShow()` | ✅ 可复用 | 通用最大化窗口直到图片出现功能 |
| `utils/GUIUtils.py` | `ShowMessageBox()` | ✅ 可复用 | 通用消息框弹出功能 |
| `utils/GUIUtils.py` | `Show_Alert()` | ✅ 可复用 | 通用警告框弹出功能 |

### 2. 系统操作模块

| 文件路径 | 函数/模块 | 标记 | 复用说明 |
|---------|---------|---------|---------|
| `utils/OSUtils.py` | `kill_APPS()` | ✅ 可复用 | 通用关闭指定应用程序功能 |
| `utils/OSUtils.py` | `seconds_to_hms()` | ✅ 可复用 | 通用秒数转换为时分秒功能 |
| `utils/NetUtills.py` | `continue_Ping()` | ✅ 可复用 | 通用网络连通性检测功能 |
| `utils/ProcessCheck.py` | `Continue_CheckAlpha()` | ⚠️ 部分可复用 | 进程监控逻辑可复用，但Alpha系统检测部分需修改 |
| `utils/ScheduleUtils.py` | `ControlSchedule()` | ✅ 可复用 | 通用Windows计划任务控制功能 |
| `utils/SreenRecord.py` | `Record()` | ✅ 可复用 | 通用屏幕录制功能 |

### 3. 工具模块

| 文件路径 | 函数/模块 | 标记 | 复用说明 |
|---------|---------|---------|---------|
| `utils/Wechat.py` | `Send()` | ✅ 可复用 | 通用微信消息发送功能 |
| `utils/SendMails.py` | `SendEmail()` | ✅ 可复用 | 通用邮件发送功能 |
| `controllers/webOp.py` | `DownloadFromParts_logistics()` | ⚠️ 部分可复用 | 文件下载逻辑可复用，但部品物流系统接口部分需修改 |
| `controllers/OutBoundList.py` | `ExcuteExcel()` | ⚠️ 部分可复用 | Excel处理逻辑可复用，但业务规则部分需修改 |

## 二、特定软件操作逻辑（废弃代码）

### 1. Alpha系统特定操作

| 文件路径 | 函数/模块 | 标记 | 废弃说明 |
|---------|---------|---------|---------|
| `controllers/ExcuteAlpha.py` | `LoginAlpha()` | ❌ 废弃 | 特定于Alpha系统的登录流程 |
| `controllers/ExcuteAlpha.py` | `ALPHA_OUTBOUND()` | ❌ 废弃 | 特定于Alpha系统的出库处理流程 |
| `controllers/ExcuteAlpha.py` | `ALPHA_OUTBOUND_Write_Off()` | ❌ 废弃 | 特定于Alpha系统的销账处理流程 |
| `controllers/ExcuteAlpha.py` | `input_KA5_Instruction_ByDF()` | ❌ 废弃 | 特定于Alpha系统KA5模块的出库指示输入 |
| `controllers/ExcuteAlpha.py` | `search_KA7_ByDF()` | ❌ 废弃 | 特定于Alpha系统KA7模块的出库进度查询 |
| `controllers/ExcuteAlpha.py` | `input_KA8_OutBound_ByDF()` | ❌ 废弃 | 特定于Alpha系统KA8模块的出库登记 |

### 2. 业务特定操作

| 文件路径 | 函数/模块 | 标记 | 废弃说明 |
|---------|---------|---------|---------|
| `OutBoundFixAssemble.py` | `main()` | ❌ 废弃 | 特定于当前出库业务的主流程 |
| `config/ImgConfig.py` | 所有图片路径配置 | ❌ 废弃 | 特定于Alpha系统界面的图片路径配置 |

## 三、代码复用建议

### 1. 立即复用
- GUI操作模块的所有函数可直接复用
- 系统操作模块的大部分函数可直接复用
- 工具模块的微信、邮件发送函数可直接复用

### 2. 修改后复用
- 进程监控、计划任务控制等函数需根据新需求修改
- 文件下载、Excel处理等函数需剥离业务规则，保留通用逻辑

### 3. 完全废弃
- 所有与Alpha系统特定界面、业务流程相关的代码
- 所有特定于当前出库业务的主流程代码