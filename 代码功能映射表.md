# 代码功能映射表

## 1. 主程序入口

| 文件路径 | 函数/模块 | 功能描述 | 依赖关系 | 复用潜力 |
|---------|---------|---------|---------|---------|
| `main.py` | `main()` | 程序入口，启动网络检测、进程监控、屏幕录制和出库处理 | `NetUtills`, `ProcessCheck`, `SreenRecord`, `OutBoundFixAssemble` | 低（特定于当前项目结构） |

## 2. 核心业务逻辑

| 文件路径 | 函数/模块 | 功能描述 | 依赖关系 | 复用潜力 |
|---------|---------|---------|---------|---------|
| `OutBoundFixAssemble.py` | `main()` | 出库处理主流程，包括Excel下载、处理、Alpha系统操作 | `webOp`, `OutBoundList`, `ExcuteAlpha`, `GUIUtils`, `OSUtils`, `Wechat`, `ScheduleUtils` | 低（特定于Alpha系统业务） |

## 3. 控制器模块

| 文件路径 | 函数/模块 | 功能描述 | 依赖关系 | 复用潜力 |
|---------|---------|---------|---------|---------|
| `controllers/ExcuteAlpha.py` | `LoginAlpha()` | Alpha系统登录流程 | `GUIUtils`, `ImgConfig`, `appConfig` | 中（可复用为通用登录模板） |
| `controllers/ExcuteAlpha.py` | `ALPHA_OUTBOUND()` | Alpha系统出库处理主流程 | `input_KA5_Instruction_ByDF`, `search_KA7_ByDF`, `input_KA8_OutBound_ByDF` | 低（特定于Alpha系统业务） |
| `controllers/ExcuteAlpha.py` | `input_KA5_Instruction_ByDF()` | KA5出库指示输入 | `GUIUtils`, `ImgConfig` | 低（特定于Alpha系统KA5模块） |
| `controllers/OutBoundList.py` | `ExcuteExcel()` | Excel文件处理 | `pandas`, `appConfig` | 中（可复用为通用Excel处理模板） |
| `controllers/webOp.py` | `DownloadFromParts_logistics()` | 从部品物流系统下载Excel | `requests`, `appConfig` | 中（可复用为通用文件下载模板） |

## 4. 工具模块

| 文件路径 | 函数/模块 | 功能描述 | 依赖关系 | 复用潜力 |
|---------|---------|---------|---------|---------|
| `utils/GUIUtils.py` | `clickByImg()` | 图片定位点击 | `pyautogui` | 高（通用GUI操作） |
| `utils/GUIUtils.py` | `waitUntilShow()` | 等待图片出现 | `pyautogui` | 高（通用GUI操作） |
| `utils/GUIUtils.py` | `ShowMessageBox()` | 弹出消息框 | `pyautogui` | 高（通用GUI操作） |
| `utils/OSUtils.py` | `kill_APPS()` | 关闭指定应用程序 | `psutil` | 高（通用系统操作） |
| `utils/OSUtils.py` | `seconds_to_hms()` | 秒数转换为时分秒 | - | 高（通用工具函数） |
| `utils/NetUtills.py` | `continue_Ping()` | 网络连通性检测 | `subprocess` | 高（通用网络操作） |
| `utils/ProcessCheck.py` | `Continue_CheckAlpha()` | Alpha进程监控 | `psutil` | 中（可复用为通用进程监控模板） |
| `utils/ScheduleUtils.py` | `ControlSchedule()` | 计划任务控制 | `win32com.client` | 中（可复用为通用计划任务模板） |
| `utils/Wechat.py` | `Send()` | 微信消息发送 | `itchat` | 高（通用消息通知） |
| `utils/SreenRecord.py` | `Record()` | 屏幕录制 | `pyautogui`, `cv2` | 高（通用屏幕操作） |

## 5. 配置模块

| 文件路径 | 函数/模块 | 功能描述 | 依赖关系 | 复用潜力 |
|---------|---------|---------|---------|---------|
| `config/appConfig.py` | 配置参数 | 系统配置参数（路径、账号、密码等） | - | 高（通用配置模板） |
| `config/ImgConfig.py` | 图片路径配置 | 界面元素图片路径配置 | - | 中（可复用为通用图片配置模板） |

## 6. 依赖关系图

```
main.py → OutBoundFixAssemble.py → controllers/ExcuteAlpha.py
                                → controllers/OutBoundList.py
                                → controllers/webOp.py
                                → utils/GUIUtils.py
                                → utils/OSUtils.py
                                → utils/Wechat.py
                                → utils/ScheduleUtils.py
                                → config/appConfig.py
                                → config/ImgConfig.py
```