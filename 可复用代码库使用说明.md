# 可复用代码库使用说明

## 一、代码库结构

```
modules/
├── base/           # 基础功能模块
│   ├── gui_utils.py    # GUI操作工具
│   ├── os_utils.py     # 系统操作工具
│   └── net_utils.py    # 网络操作工具
├── llm/            # 大模型调用模块
│   └── llm_interface.py # 大语言模型接口
├── agent/          # Agent模块
│   └── agent_core.py   # Agent核心逻辑
├── skill/          # Skill模块
│   └── skill_interface.py # 技能接口
├── monitor/        # 记录监控模块
│   └── monitor_core.py # 监控核心逻辑
└── test/           # 测试模块
    └── test_framework.py # 测试框架
```

## 二、各模块使用说明

### 2.1 基础功能模块

#### 2.1.1 GUI操作工具 (gui_utils.py)

**功能**：实现Windows桌面应用的GUI操作，包括图片定位点击、等待图片出现等

**使用示例**：
```python
from modules.base.gui_utils import GUIUtils

# 点击图片
GUIUtils.clickByImg(logger, "button.png", offsetx=10, offsety=10, confidence=0.9)

# 等待图片出现
position = GUIUtils.waitUntilShow(logger, "loading.png", timeout=20)

# 最大化窗口
GUIUtils.maxWindowUntilShow(logger, "window_title.png", timeout=10)
```

#### 2.1.2 系统操作工具 (os_utils.py)

**功能**：实现Windows系统操作，包括进程管理、系统资源监控等

**使用示例**：
```python
from modules.base.os_utils import OSUtils

# 关闭指定应用程序
OSUtils.killAPPS(["notepad.exe", "chrome.exe"])

# 检查进程是否在运行
is_running = OSUtils.isProcessRunning("notepad.exe")

# 获取CPU使用率
cpu_usage = OSUtils.getCPUUsage()
```

#### 2.1.3 网络操作工具 (net_utils.py)

**功能**：实现网络操作，包括ping命令、网络连接检查等

**使用示例**：
```python
from modules.base.net_utils import NetUtils

# 执行ping命令
success, output = NetUtils.ping("www.baidu.com", count=4, timeout=1000)

# 持续ping
NetUtils.continuePing("www.baidu.com", interval=60)
```

### 2.2 大模型调用模块

#### 2.2.1 大语言模型接口 (llm_interface.py)

**功能**：实现与各类大语言模型的集成，包括OpenAI、文心一言等

**使用示例**：
```python
from modules.llm.llm_interface import LLMFactory

# 创建OpenAI客户端
openai_client = LLMFactory.createClient(
    "openai",
    api_key="your_api_key",
    model="gpt-3.5-turbo"
)

# 生成文本
text = openai_client.generateText("请写一篇关于RPA的文章")

# 对话补全
messages = [
    {"role": "system", "content": "你是一个RPA专家"},
    {"role": "user", "content": "请介绍RPA的应用场景"}
]
response = openai_client.chatCompletion(messages)
```

### 2.3 Agent模块

#### 2.3.1 Agent核心逻辑 (agent_core.py)

**功能**：实现任务规划、资源调度与异常处理

**使用示例**：
```python
from modules.agent.agent_core import AgentCore, TaskScheduler

# 创建Agent实例
agent = AgentCore(max_workers=5)

# 启动Agent
agent.start()

# 注册技能
class MySkill:
    def execute(self, params):
        return f"执行技能成功，参数: {params}"

agent.registerSkill("my_skill", MySkill)

# 调度任务
task_id = TaskScheduler.scheduleTask(
    agent,
    "my_skill",
    {"param1": "value1", "param2": "value2"},
    priority=1
)

# 获取任务状态
task_status = agent.getTaskStatus(task_id)
```

### 2.4 Skill模块

#### 2.4.1 技能接口 (skill_interface.py)

**功能**：实现可扩展的技能库，支持动态加载

**使用示例**：
```python
from modules.skill.skill_interface import SkillManager, SkillInterface

# 创建技能管理器
skill_manager = SkillManager()

# 加载技能
skill_manager.loadSkill("my_skill.py")

# 列出所有技能
skills = skill_manager.listSkills()

# 执行技能
skill = skill_manager.getSkill("my_skill")
result = skill.execute({"param": "value"})
```

### 2.5 记录监控模块

#### 2.5.1 监控核心逻辑 (monitor_core.py)

**功能**：实现操作日志记录、性能监控与异常报警

**使用示例**：
```python
from modules.monitor.monitor_core import MonitorSystem

# 创建监控系统
monitor = MonitorSystem()

# 启动监控
monitor.start()

# 记录日志
monitor.logInfo("操作成功")
monitor.logError("操作失败")

# 处理异常
monitor.handleException("task_123", Exception("任务执行失败"))

# 记录操作
monitor.recordOperation("click", {"img": "button.png"}, "success")
```

### 2.6 测试模块

#### 2.6.1 测试框架 (test_framework.py)

**功能**：实现单元测试、集成测试与系统测试

**使用示例**：
```python
from modules.test.test_framework import TestFramework

# 创建测试框架
framework = TestFramework()

# 添加单元测试
def testAddition():
    assert 1 + 1 == 2

framework.addUnitTest("加法测试", testAddition)

# 运行所有测试
results = framework.runAllTests()
```

## 三、代码复用建议

### 3.1 优先复用的模块

1. **基础功能模块**：GUI操作、系统操作、网络操作等通用功能
2. **记录监控模块**：日志记录、性能监控等通用功能
3. **测试模块**：测试框架等通用功能

### 3.2 按需复用的模块

1. **大模型调用模块**：根据需要集成大语言模型
2. **Agent模块**：根据需要实现任务调度
3. **Skill模块**：根据需要开发可扩展技能

## 四、注意事项

1. **依赖安装**：确保已安装所有依赖库，如pyautogui、psutil、openai等
2. **权限问题**：部分操作需要管理员权限，如关闭系统进程
3. **性能优化**：避免频繁调用GUI操作，可适当添加等待时间
4. **异常处理**：所有操作都应添加异常处理，确保系统稳定性

## 五、常见问题

### 5.1 图片定位失败
- 检查图片路径是否正确
- 调整confidence参数（默认0.9）
- 确保图片在屏幕上可见

### 5.2 大模型调用失败
- 检查API密钥是否正确
- 确保网络连接正常
- 调整模型参数（如temperature、max_tokens）

### 5.3 任务调度失败
- 检查技能是否已注册
- 确保Agent已启动
- 检查任务参数是否正确